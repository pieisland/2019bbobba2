#시간대별로 입양이 몇 건이나 발생했는지 조회
SELECT HOUR(DATETIME) AS HOUR, COUNT(HOUR(DATETIME)) AS COUNT
FROM ANIMAL_OUTS
#GROUP BY DATETIME
#WHERE HOUR(DATETIME)>= 9 AND HOUR(DATETIME)<=19
WHERE HOUR(DATETIME) BETWEEN 9 AND 19
GROUP BY HOUR(DATETIME)
ORDER BY HOUR(DATETIME)

->HOUR을 쓰면 DATETIME 형식 데이터에서 시간만 가지고 올 수 있다
그리고 GROUP BY와 ORDER BY는 순서가 중요한 것 같다. WHERE 절이 위에 있어야 되고..
BETWEEN A AND B 는 = 들어간 형태와 동일하다.
