#include<cstdio>
#include<iostream>
#include<vector>
#include<algorithm>
#include<string>
#include<cstring>
#include<queue>
using namespace std;

/*
2019.07.02.화.
*/
int main(){
	
	cin.tie(NULL);
	int n;
	cin >> n;

	for (int i = 1; i <= n; i++)
	{
		if (i == 1)
		{
			for (int j = 1; j <= n - i; j++)
				cout << " ";
			cout << "*\n";
		}
		else if (i == n)
		{
			for (int j = 1; j <= 2 * n - 1; j++)
				cout << "*";
			cout << "\n";
		}
		else
		{
			for (int j = 1; j <= n - i; j++)
				cout << " ";
			cout << "*";
			for (int j = 1; j <= 2 * i - 3; j++)
				cout << " ";
			cout << "*\n";
		}
	}

	return 0;
}
